document.addEventListener('DOMContentLoaded', () => {
  const btn = document.querySelector('.ui-button');
  btn?.addEventListener('click', () => alert('Botão clicado — integração front/backend virá em próximas tarefas'));
});
